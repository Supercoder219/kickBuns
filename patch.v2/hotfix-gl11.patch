--- a/src/desktop/main.c
+++ b/src/desktop/main.c
@@ -118,10 +118,41 @@
 }
 
 #if defined(ENABLE_LEGACY_GL) || defined(ENABLE_MODERN_GL) || ((defined(USE_GLFW3) || defined(USE_GLFW2)) && defined(ENABLE_SW_RENDERER))
+#ifdef PLATFORM_MACOS9
+/*
+ * Apple OpenGL 1.2.1 drivers vary wildly by GPU. Undertale's legacy path only
+ * needs texture unit zero and ordinary additive blending, so provide safe GL
+ * 1.1 fallbacks when Rage-era drivers omit later ARB/EXT entry points.
+ */
+static void APIENTRY macCompatActiveTexture(GLenum texture) {
+    (void)texture;
+}
+
+static void APIENTRY macCompatClientActiveTexture(GLenum texture) {
+    (void)texture;
+}
+
+static void APIENTRY macCompatBlendFuncSeparate(GLenum srcRGB, GLenum dstRGB,
+                                                 GLenum srcAlpha, GLenum dstAlpha) {
+    (void)srcAlpha;
+    (void)dstAlpha;
+    if (glad_glBlendFunc) glad_glBlendFunc(srcRGB, dstRGB);
+}
+
+static void APIENTRY macCompatBlendEquation(GLenum mode) {
+    /* GL 1.1's fixed blend equation is GL_FUNC_ADD, Undertale's normal mode. */
+    (void)mode;
+}
+#endif
+
 static bool platformInitGlad(void) {
     glGetString = (PFNGLGETSTRINGPROC)platformGetProcAddress("glGetString");
-    if (!glGetString)
-        return 0;
+    if (!glGetString) {
+#ifdef PLATFORM_MACOS9
+        MacTrace_stage("OpenGL failure: glGetString unavailable");
+#endif
+        return false;
+    }
 
     fprintf(stderr, "OpenGL Version: %s\n", (const char*)glGetString(GL_VERSION));
     GLVer ver = GLCommon_getGLVersion();
@@ -150,10 +181,21 @@
     if (!glad_glBlendEquation)
         glad_glBlendEquation = (PFNGLBLENDEQUATIONPROC)platformGetProcAddress("glBlendEquation");
 
-    if (!glad_glActiveTexture || !glad_glBlendFuncSeparate || !glad_glBlendEquation) {
-        fprintf(stderr, "Mac OS 9: The OpenGL driver lacks required multitexture/blend extensions.\n");
-        return false;
+    if (!glad_glActiveTexture) {
+        glad_glActiveTexture = macCompatActiveTexture;
+        MacTrace_stage("OpenGL compatibility: single texture unit fallback");
+    }
+    if (!glad_glClientActiveTexture)
+        glad_glClientActiveTexture = macCompatClientActiveTexture;
+    if (!glad_glBlendFuncSeparate) {
+        glad_glBlendFuncSeparate = macCompatBlendFuncSeparate;
+        MacTrace_stage("OpenGL compatibility: basic blend-function fallback");
+    }
+    if (!glad_glBlendEquation) {
+        glad_glBlendEquation = macCompatBlendEquation;
+        MacTrace_stage("OpenGL compatibility: additive blend-equation fallback");
     }
+    MacTrace_stage("OpenGL symbol loading complete");
 #endif
 
     return true;
--- a/src/gl_legacy/gl_legacy_renderer.c
+++ b/src/gl_legacy/gl_legacy_renderer.c
@@ -85,7 +85,8 @@
 #ifdef PLATFORM_PS3
     return true;
 #else
-    return (glGenFramebuffers && glBlitFramebuffer);
+    return glGenFramebuffers && glDeleteFramebuffers && glBindFramebuffer &&
+           glFramebufferTexture2D && glCheckFramebufferStatus && glBlitFramebuffer;
 #endif
 }
 
--- a/src/audio/macos9/mac_audio_system.c
+++ b/src/audio/macos9/mac_audio_system.c
@@ -99,6 +99,7 @@ typedef struct MacAudioSystem {
     AudioSystem base;
     FileSystem* fileSystem;
     SndChannelPtr channel;
+    SndDoubleBackUPP doubleBackUPP;
     SndDoubleBufferHeader header;
     SndDoubleBufferPtr buffers[2];
     volatile bool bufferNeedsFill[2];
@@ -373,7 +374,7 @@ static void fillOutputBuffer(MacAudioSystem* mac, SndDoubleBufferPtr buffer) {
     buffer->dbFlags = dbBufferReady;
 }
 
-static void macDoubleBack(SndChannelPtr channel, SndDoubleBufferPtr buffer) {
+static pascal void macDoubleBack(SndChannelPtr channel, SndDoubleBufferPtr buffer) {
     MacAudioSystem* mac = gMacAudio;
     (void)channel;
     if (mac == NULL || buffer == NULL) return;
@@ -451,7 +452,17 @@ static void macInit(AudioSystem* audio, DataWin* dataWin, FileSystem* fileSystem
     mac->header.dbhCompressionID = 0;
     mac->header.dbhPacketSize = 0;
     mac->header.dbhSampleRate = (Fixed)((UInt32)MAC_AUDIO_OUTPUT_RATE << 16);
-    mac->header.dbhDoubleBack = (SndDoubleBackUPP)macDoubleBack;
+
+    /* PowerPC CFM callbacks must be wrapped in a Routine Descriptor. */
+    mac->doubleBackUPP = NewSndDoubleBackUPP(macDoubleBack);
+    if (mac->doubleBackUPP == NULL) {
+        fprintf(stderr, "Mac audio: NewSndDoubleBackUPP failed; continuing without sound\n");
+        MacTrace_stage("audio init failed: callback UPP allocation");
+        SndDisposeChannel(mac->channel, true);
+        mac->channel = NULL;
+        return;
+    }
+    mac->header.dbhDoubleBack = mac->doubleBackUPP;
 
     for (i = 0; i < 2; ++i) {
         mac->buffers[i] = (SndDoubleBufferPtr)calloc(1, bufferBytes);
@@ -474,6 +485,7 @@ static void macInit(AudioSystem* audio, DataWin* dataWin, FileSystem* fileSystem
     if (err != noErr) {
         fprintf(stderr, "Mac audio: SndPlayDoubleBuffer failed (%d); continuing without sound\n", (int)err);
         MacTrace_stage("audio init failed: SndPlayDoubleBuffer");
+        gMacAudio = NULL;
         return;
     }
     mac->initialized = true;
@@ -487,7 +499,14 @@ static void macDestroy(AudioSystem* audio) {
     int i;
     gMacAudio = NULL;
     for (i = 0; i < MAC_AUDIO_MAX_VOICES; ++i) releaseVoice(&mac->voices[i]);
-    if (mac->channel != NULL) SndDisposeChannel(mac->channel, true);
+    if (mac->channel != NULL) {
+        SndDisposeChannel(mac->channel, true);
+        mac->channel = NULL;
+    }
+    if (mac->doubleBackUPP != NULL) {
+        DisposeSndDoubleBackUPP(mac->doubleBackUPP);
+        mac->doubleBackUPP = NULL;
+    }
     for (i = 0; i < 2; ++i) free(mac->buffers[i]);
     for (i = 0; i < MAC_AUDIO_MAX_STREAMS; ++i) free(mac->streams[i].path);
     if (arrlen(mac->base.audioGroups) > 1) {
